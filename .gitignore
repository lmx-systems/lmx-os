__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
*.egg-info/
dist/
build/
.DS_Store

# Local tool settings (sandbox/IDE artifacts, never repo content)
.claude/

# Business documents & brand source creatives live in docs/ for convenience
# but are deliberately NOT versioned with the code (Sourabh's call, July
# 2026) - the shared drive is their source of truth. Product-embedded
# brand assets (dashboard/public/, client-portal/public/,
# driver-app/assets/, app/billing/assets/) ARE committed.
docs/*.pptx
docs/*.docx
docs/*.pdf
docs/*.zip
docs/LMX branding */
docs/~$*
