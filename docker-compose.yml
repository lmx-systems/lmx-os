version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lmx_os}
      POSTGRES_USER: ${POSTGRES_USER:-lmx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me}
    ports:
      - "5432:5432"
    volumes:
      - lmx_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lmx}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-lmx}:${POSTGRES_PASSWORD:-change_me}@postgres:5432/${POSTGRES_DB:-lmx_os}
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"

  dashboard:
    build:
      context: ./dashboard
    depends_on:
      - app
    environment:
      # Injected at container START (roadmap item D2, docker/
      # 40-runtime-config.sh) - change these and `docker compose up -d`,
      # no image rebuild. Must be reachable from the user's BROWSER, not
      # from inside the Docker network.
      API_BASE_URL: ${DASHBOARD_API_BASE_URL:-http://localhost:8000}
      # Must match the app service's API_SHARED_SECRET (from .env), if set.
      API_SHARED_SECRET: ${API_SHARED_SECRET:-}
    ports:
      - "5173:80"

  client-portal:
    build:
      context: ./client-portal
    depends_on:
      - app
    environment:
      # Injected at container START (roadmap item D2) - no shared secret
      # here, unlike dashboard: /client/* has its own real per-client JWT
      # auth (app/client_auth/), not the internal ops shared-secret stopgap.
      API_BASE_URL: ${CLIENT_PORTAL_API_BASE_URL:-http://localhost:8000}
    ports:
      - "5174:80"

volumes:
  lmx_pg_data:
