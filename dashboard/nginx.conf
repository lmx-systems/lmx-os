server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback - no client-side routing yet, but this is the standard
    # shape and avoids a rewrite later if routes are added.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
