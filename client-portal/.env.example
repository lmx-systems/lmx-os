VITE_API_BASE_URL=http://localhost:8000

# No shared-secret var here, unlike dashboard/.env.example - /client/* has
# its own real per-client JWT auth (app/client_auth/), not the internal
# ops shared-secret stopgap. See app/security.py's EXEMPT_PREFIXES.
