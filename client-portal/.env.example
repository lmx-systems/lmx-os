VITE_API_BASE_URL=http://localhost:8000

# No ops-login var here, unlike dashboard/.env.example - /client/* has its
# own real per-client JWT auth (app/client_auth/), a separate auth domain
# from the internal ops-dashboard login. See app/ops_auth/middleware.py's
# EXEMPT_PREFIXES.
