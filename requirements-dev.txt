-r requirements.txt
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
# Pinned exactly (not >=) so CI's lint gate is reproducible locally: an
# unbounded floor let CI install a newer ruff whose default ruleset had
# drifted from what developers saw, turning the gate red on rules no local
# run surfaced. Bump deliberately, re-running `ruff check app tests` first.
ruff==0.15.22
mypy>=1.10.0
fakeredis>=2.23.2
# Reading back the PDFs app/settle/pdf.py writes, so a test can assert what a
# customer would actually see on the page rather than that some bytes were
# produced. It was already present in the venv as a transitive dependency and
# undeclared, which is the shape of a suite that is green locally and red in CI.
pypdf>=5.0.0
