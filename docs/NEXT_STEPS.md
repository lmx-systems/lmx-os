# LMX OS — Next Steps Tracker

Living punch list. Update the Status column as items move — this file is
meant to be edited directly (by hand or by Claude Code), not regenerated.

Last synced with `docs/ARCHITECTURE.md`: see that file for the full technical
detail behind each item below.

**See `docs/ROADMAP.md`** for the consolidated view: every open item across
the whole system in one place (not just this row-by-row backlog), plus the
phased plan for what comes after item 16 below to get LMX OS fully running
at Hub 1.

## Engineering

| # | Item | Why it matters | Status |
|---|---|---|---|
| 1 | Stand up real Postgres + Redis, run the migration, add integration tests against a live DB | Every test today runs offline (fakeredis + pure functions) — zero integration coverage against a real database. Biggest testing gap in the codebase. | Done — `tests/integration/` (migration, ingestion, fleet state, full pipeline), wired into CI with real service containers + a skip-detection safeguard. Caught and fixed a real timezone bug in `Order`/`Stop` models (see `docs/ARCHITECTURE.md`). |
| 2 | Confirm the real Epicor payload shape with the first design-partner client, update `EpicorAdapter` | Field names (`OrderNum`, `ShipToNum`, etc.) are a guess, not verified against a real tenant. Peer review calls Epicor config drift "the most common cause of Phase 1 slippage." | Not started — `demo/` now has a placeholder sample payload + runnable seed/send scripts for demoing the pipeline (e.g. to investors) while a real design-partner integration is still pending. Not a substitute for verifying the real shape. |
| 3 | Check the batch-hold "4-question decision logic" and SLA hold-window minutes against the Source of Truth Index (Google Drive) | Both were reconstructed from the peer-review summary, not the canonical doc, which wasn't reachable while building. Isolated in `app/batch_queue/queue.py` and `app/sla/engine.py` so fixing is contained. | Not started |
| 4 | Provision a Google Cloud service account (`roles/cloudoptimization.user`), set `GOOGLE_APPLICATION_CREDENTIALS` + `GOOGLE_CLOUD_PROJECT_ID`, run one real `optimizeTours` call | The real client is now implemented (`app/optimizer/google_routes_client.py`) but has never been exercised against the live API — request/response mapping and the per-tier skip-penalty values are unverified. | In progress — implemented, not yet verified live |
| 5 | Build the nightly pattern-detection job that populates `proposed_rules` (component 6) | Schema exists (`stop_flags`, `proposed_rules`, `active_rules`); no job writes to it yet. | Done — `app/learning_loop/`, endpoint `/learning-loop/{hub_id}/run-nightly-job`. Flag-type naming convention (`hold_window_too_short`/`_too_long`) still needs sign-off from whoever builds the driver app. |
| 6 | Decide event-bus vs. manual trigger for the Dispatch Optimizer *and* the Learning Loop's nightly job | Design doc calls for re-optimizing on "every meaningful event" (order released, driver status change, stop completed), and the nightly job needs an actual schedule. | Partially done — Dispatch Optimizer: done. `app/events/bus.py` + `app/optimizer/event_trigger.py` wire real events: order ingestion publishes `order_held`, driver status changes publish `driver_status_changed`. `stop_completed` has no producer yet (component 7 / driver app not built). Bus is in-process only — a real gap if the app ever runs as more than one instance. Learning Loop nightly job: still not started, remains manual-only (`/learning-loop/{hub_id}/run-nightly-job`) — needs a real scheduler. |
| 9 | **Add real authentication/authorization to the API** | Every endpoint is wide open. Was a standing gap; now urgent because `dashboard/` gives it a clickable UI — anyone who can reach the API can see all fleet/order data and trigger optimizer or learning-loop cycles for any hub. CORS restricts which browser origins can call in, which is not access control. | Done — the shared-secret stopgap described in earlier versions of this row is retired. `app/ops_auth/middleware.py`'s `OpsUserAuthMiddleware` gates every endpoint except `/health`, API docs, and the driver/client/webhook surfaces (each with their own real auth) behind a real per-account Bearer JWT (`app/ops_auth/`, `app/models/ops_user.py`, migration `0011`), same password+JWT shape as the client portal. `dashboard/` has a real login screen (`LoginPage.tsx`) backed by `POST /ops/auth/login`; `scripts/create_ops_user.py --role admin\|viewer` bootstraps accounts (or resets a password/reactivates one), since there's no self-service signup by design. A real `admin`/`viewer` role model now exists too (migration `0012`): `require_admin` (`app/ops_auth/dependencies.py`) gates the mutating endpoints (run-cycle, run-nightly-job, onboard a client, revoke a driver device, run payroll), and the dashboard hides those controls entirely for a viewer rather than showing them disabled. Live-verified end to end with real admin and viewer accounts over real HTTP and in a real browser. Still just two roles, not a full permissions matrix. |
| 10 | Orchestrator dashboard (component 7, partial) | Internal Vite/React/TS/Tailwind SPA (`dashboard/`) — fleet overview, hold queue, order summary, manual optimizer/learning-loop triggers. Client dashboard, driver mobile app, and shop SMS (the rest of component 7) are separate, larger builds, not started. | Done — redesigned as a KPI-first console (approved mockup, then implemented). Hub-list endpoint and runtime (not build-time) API URL are also done — see `docs/ARCHITECTURE.md`'s "Orchestrator dashboard" section. Remaining gaps: no shop name/cluster-mate on held orders, no driver display name, no server-side last-cycle telemetry. |
| 11 | Enrich `HeldOrderView` with shop name + cluster-mate ids; add `name` to `DriverState`; add a "last cycle" snapshot endpoint | Follow-ups the dashboard redesign surfaced — the mockup assumed this data, the current API doesn't return it (see item 10's gaps in `docs/ARCHITECTURE.md`). Not blocking, but the dashboard is visibly working around each one. | Done — all three closed and verified (78 tests passing, live demo re-run confirmed driver name + last-cycle telemetry). Dashboard updated to use all three. |
| 12 | Driver app Phase 1: real driver auth, job-offer/accept model, Route/Stop API, React Native core loop | The wireframe spec (`LMX Driver App Wireframes.dc.html`) needed a backend that barely existed — no per-driver auth, no accept/decline concept, zero Route/Stop endpoints despite the tables existing, and the customer/dropoff side of an order was never modeled at all. See `docs/ARCHITECTURE.md`'s "Driver app" section for the full gap analysis and what's simplified in v1. | Done — `app/driver_auth/`, `app/api/driver_routes.py`, `app/models/route_offer.py`, `driver-app/` (Expo/React Native, screens 1a-1m). 87 backend tests passing (up from 78); `tsc --noEmit` clean on the app. Screens 1n-1r (earnings, messaging, full profile) are Phase 2/3, not started — no backend exists for a payout ledger or real Twilio SMS/masked calling yet, and both are sized as their own projects, not extensions of this pass. |
| 13 | Driver app Phase 2: profile screen (vehicle edit, documents, payment method) | Screen 1r, minus earnings (explicitly deferred by Sourabh — no real pay formula to build from, see below). | Done — `app/models/driver_document.py`, `PUT /driver/me/payment-method` (last-4-only, no payout rail), `GET`/`PUT /driver/me/documents`, going-online now 409s on an expired/missing document. `driver-app/` restructured to bottom tabs (Home/Profile); new ProfileScreen, EditVehicleScreen, DocumentsScreen, PaymentMethodScreen. 104/105 backend tests passing — the 1 failure is a confirmed local-sandbox-only artifact (test Redis via `redislite` is v6.2.14, missing `EXPIRE...NX`; production is `redis:7-alpine`, which has it), not a real bug. `tsc --noEmit` clean. `trip_count` is a real count of completed Routes; no star rating anywhere, since there's no rating-submission system to source one from. |
| 14 | Driver app Phase 3: earnings (screens 1n/1o) and messaging/support (1p/1q) | Deliberately out of scope for Phase 2 (Sourabh: "skip earnings for now"). Earnings needs either a real pay formula (none exists — no fare/price field anywhere in `Order`/`Route`/`Stop`) or an explicit placeholder decision; messaging needs a real infrastructure choice (Twilio masked calling/SMS, in-app chat, or something else) that hasn't been made yet. | Done, with real open items below — `app/models/message.py`, `app/messaging/sms_client.py` (Twilio real client + stub fallback, no Twilio account provisioned yet), `app/api/webhooks.py` (`/webhooks/twilio/inbound-sms`), `GET /driver/me/earnings`, `GET /driver/me/trips`, `POST /driver/stops/{id}/message-customer`, `POST`/`GET /driver/me/messages`. `driver-app/` gained a 3rd bottom tab (Earnings) plus MessageCustomerScreen (wired into the active-job screen's "Message" button, replacing the old inert alert) and SupportScreen (from Profile). 114/115 backend tests passing (the 1 failure is the same confirmed sandbox-only Redis-version artifact as item 13, unrelated to this work); `tsc --noEmit` clean. Sourabh's calls: pay formula is an explicit placeholder ($18/hr flat, clearly labeled "estimate, not final pay" in the UI and API), payroll goes through ADP/Gusto once one is chosen and provisioned (neither is set up yet — item 15), messaging is masked SMS via Twilio for both customer contact and dispatch/support contact. Real gaps, not stylistic choices: ~~(a) the inbound Twilio webhook has no request-signature verification yet~~ — **closed**: `app/messaging/twilio_signature.py` verifies `X-Twilio-Signature` (enforced once `TWILIO_AUTH_TOKEN` is configured), with a `TWILIO_WEBHOOK_BASE_URL` override for the eventual reverse-proxy case. ~~(b) inbound-reply matching is by phone number only, no session concept~~ — **hardened**: `_find_matching_thread` (`app/api/webhooks.py`) now infers channel from `From` before matching (a support reply can never land in a customer thread or vice versa - previously true even across different drivers, since every driver's support messages share one `counterparty_phone`), prefers threads with no inbound reply already recorded, and for customer channel additionally requires the stop still be non-terminal. Real, honestly-flagged remaining limitation: two genuinely concurrent, still-unanswered threads to the same number can't be told apart from phone number alone - that needs either a Twilio Proxy-style number-per-conversation pool or a reference code in the reply body, neither of which exists; this case is now logged (`inbound_sms_ambiguous_match`) rather than silently guessed at. (c) masked *voice* calling (as opposed to SMS) is still not built — the active-job screen's "Call" button says so explicitly now instead of showing a generic "not wired up" alert. |
| 15 | Provision a real payroll account | Item 22 below picked **Rippling** (not ADP/Gusto — handles W2 payroll and 1099 contractor payments on one platform, which reuses the same integration across both worker classifications) and built the `PayrollProvider` interface + real hours/overtime engineering against it. What's left is purely procurement: a Rippling account, API credentials, and confirming the real API shape (`app/payroll/rippling_client.py`'s endpoint path/payload is an unverified best-effort guess, same status as the Epicor adapter) — no more engineering is worth doing here until that exists. | Not started (engineering behind it: Done — see item 22) |
| 16 | Provision a real Twilio account/phone number | Item 14's messaging (masked SMS) runs against `StubSmsClient` until `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_FROM_NUMBER` are set (`app/config.py`) — no messages are actually delivered today. Also needed at the same time: a `SUPPORT_PHONE_NUMBER` to configure, and pointing the Twilio number's inbound-SMS webhook at `/webhooks/twilio/inbound-sms` once that endpoint has real signature verification (see item 14's gap (a)). | Not started |
| 17 | Phase 8: HOT_SHOT tier, tiered client billing, client portal, minimal client onboarding, shop SMS | Built ahead of the `docs/ROADMAP.md` sequencing at Sourabh's request — the first client wanted a full client portal and shop SMS at MVP, not deferred. See `docs/ARCHITECTURE.md`'s new Phase 8 section for the full technical detail. | Done — migration `0007` (enum value, `client_rates`, `Client.portal_email`/`portal_password_hash`, `Order.fee_cents`); `app/sla/engine.py`, `app/batch_queue/queue.py`, `app/optimizer/google_routes_client.py`, `app/api/driver_routes.py`'s `accept_offer` all updated for HOT_SHOT; `app/ingestion/service.py` computes `fee_cents`; `app/client_auth/`, `app/api/client_routes.py`, `app/api/admin_routes.py` (new); `client-portal/` (new Vite/React/TS/Tailwind app); `app/messaging/shop_notifications.py` (new); dashboard gained `OnboardClientForm.tsx` + a HOT_SHOT tier filter/badge. Real gaps this surfaced: billing beyond a per-order `fee_cents` (invoicing, statements, payment collection) doesn't exist; HOT_SHOT's skip-penalty and hold-window values are placeholders, same status as the original three tiers. See item 18 for the two security gaps this phase left open. |
| 18 | Close Phase 8's 2 security gaps: rate-limit `/client/auth/login`, enforce `CLIENT_JWT_SECRET` ≠ `DRIVER_JWT_SECRET` | A client's `portal_email` is a fixed, guessable target (unlike a driver's rotating OTP), and a password is the first credential this codebase protects with anything other than a shared API key or a one-time code - both gaps were flagged the moment Phase 8 shipped, not discovered later. | Done — Rate limiting: `app/client_auth/login_rate_limit.py` (same "counter + NX-guarded TTL" shape as `app/driver_auth/otp_store.py`'s issuance limiter), wired into `login()` in `app/api/client_routes.py`, reset on a successful login. Secret-distinctness: `app/config.py`'s `assert_jwt_secrets_are_distinct()`, called from `app/main.py`'s lifespan alongside the existing per-secret checks — refuses to start outside `development` if `CLIENT_JWT_SECRET` and `DRIVER_JWT_SECRET` are ever set to the same real value. Verified with `tests/test_client_auth.py` (21/21 passing) and `tests/integration/test_client_portal_integration.py`'s rate-limit tests — the latter hit the same known Redis-version gap as item 13/T2 (`EXPIRE ... NX` unsupported on the sandbox's redis 6.2.14; production is `redis:7-alpine`, which supports it), so they don't pass in this sandbox, same accepted status as the driver OTP tests. |
| 19 | Redesign driver app per a separate wireframe spec: merged screens, flag-an-issue, offline write queue, device-bound biometric auth, live route-change push | The driver app built in items 12-14 didn't match an existing, separate wireframe spec (`LMX Driver App Wireframes.pdf`) discovered mid-session — a fundamentally different feel from a "gig worker" app: consolidated screens, no SLA jargon, offline-first, device-bound Face ID re-entry instead of OTP every shift, and live notification when a route changes mid-shift. | Done — **Screen merge**: `TodayRouteScreen` (merges Home/AvailableJobs/ActiveRoute) + `useTodayRoute` hook; `StopDetailScreen` (merges arrive/scan/POD). **Flag an issue**: new `Stop.failure_reason`/`flag_note`/`flagged_at` columns (migration `0008`), `StopFailureReason` enum, `POST /driver/stops/{id}/flag`, `FlagIssueScreen`. **Offline-first write queue**: `driver-app/src/offline/` (AsyncStorage FIFO outbox, optimistic UI overlay, exponential backoff) — required fixing `complete_stop` to be a safe-retry no-op instead of a 409, and fixing three route-finished checks (`Stop.status != "completed"` → `Stop.status.notin_(_TERMINAL_STOP_STATUSES)`) so a route with one flagged stop can still complete. **Device-bound biometric auth**: new `DriverDevice` model (migration `0009`), JWT gains a `device_id` claim + Redis revocation denylist, `expo-local-authentication` Face ID/Touch ID gate, `BiometricLockScreen`, `GET`/`DELETE /driver/me/devices`, `POST /driver/auth/refresh`. **Live route-change push**: `DispatchOptimizerService` can now insert a new stop into an already-active route (never touching the driver's current/already-passed stops), `GET /driver/me/route-events` (SSE via `sse-starlette`; `react-native-sse` client-side since browser `EventSource` can't send a custom `Authorization` header), `RouteChangeBanner`. Three real bugs found and fixed via live interactive testing on the iOS Simulator: optimistic-UI-revert-after-flush, stuck-on-"Loading" after a route completed elsewhere, and an unhandled promise rejection in the offer-polling loop. Real gap, an explicit choice: true selfie/liveness verification was **not** built — device-bound token + Face ID/Touch ID instead, since building real per-gig identity verification would need new photo storage/consent/verification infra nobody asked for. Item 21 below is a direct follow-up this surfaced. |
| 20 | Apply real brand assets + unify on brand green across all three apps | Dashboard/client-portal/driver-app all showed placeholder "L"/"LX" box logos, and accent colors didn't match each other (indigo on the two web apps vs. an approximate Tailwind green on the driver app) — until real creative assets (an `LMX branding` folder) and a dated brand decision (`LMX_Brand_Asset_Inventory.docx`: brand green `#0A6644`, unified everywhere, teal retired) were found in the LMX OS docs folder. | Done — a real logo mark generated from the LMX wordmark asset, applied to dashboard/client-portal favicons + TopBars + the client-portal login page, and to driver-app's app icon, Android adaptive icon, splash screen, notification icon, and sign-in screen. `--accent`/`--accent-hover`/`--accent-dim` (both web apps, light+dark) and driver-app's `primary`/`accent`/`success` tokens all retuned to the exact brand hex. Real gaps: no vector master (SVG/AI) exists for the logo — flagged as a gap in the brand doc itself — so every generated asset is raster-derived; native icon/splash/adaptive-icon/notification-icon changes only render in a real device build, not Expo Go (which always shows its own icon), so these can't be visually verified until item A6 (EAS build) exists. |
| 21 | Real vehicle-capacity tracking for mid-route insertion | Item 19's live route-change push shipped with a placeholder `MAX_STOPS_PER_ACTIVE_ROUTE` stop-count cap instead of a real weight/volume check, because `DriverState.load_units` was never actually incremented anywhere in the codebase. | Done — `complete_stop` now increments `load_units` on pickup completion and decrements it on dropoff completion; `flag_stop_issue` also decrements it when a dropoff is flagged (a flagged pickup needs no adjustment, since nothing was loaded). `DispatchOptimizerService._insert_unassigned_into_active_routes` now checks real `capacity_units - load_units` instead of a stop-count cap; a route whose driver has no fleet state on file is skipped defensively rather than assumed to have room. |
| 22 | Driver worker-classification foundation + real W2 hours/overtime/payroll-run (Phase 1 of a W2 → 1099 → gig phased rollout) | Sourabh's stated sequencing: get the app feasible/launch-ready for W2 employees (paid monthly) first, then 1099 contractors (paid weekly), then gig per-delivery workers. An analysis of the shipped design (single online/offline toggle, single-push job offers with no dollar amount shown, ops-only provisioning, device-bound trust-once auth, no per-delivery fare model anywhere) found it already fit W2/1099-hourly work well and gig work poorly — see `docs/ARCHITECTURE.md` if that analysis needs re-deriving. | In progress — `Driver.employment_type` (`w2`\|`contractor_1099`\|`gig`, defaults `w2`) and `Driver.hourly_rate_cents` (a real per-driver rate, migration `0010`). New `driver_shift_events` table + capture wired into `POST /driver/me/state` — a durable online/offline/break history, independent of Redis fleet state (which only ever holds the *current* status). `app/payroll/`: a `PayrollProvider` interface, `RipplingPayrollProvider` (real client, unexercised — no account provisioned, see item 15) + `StubPayrollProvider`. `app/payroll/hours.py` replaces the old route-span earnings heuristic with real on-duty hours reconstructed from the shift-event log (correctly excluding break time), federal FLSA overtime (1.5x over 40hrs/week) for `w2` only, and a monthly (`w2`) vs. weekly (`contractor_1099`/`gig`) pay period matching Sourabh's stated cadence. New `POST /admin/payroll/{hub_id}/run` submits the most recently completed period's hours per driver, mirroring item 5's manual-trigger pattern (no scheduler exists here either). 186 backend tests passing (up from 168 at the start of this stretch of work); `tsc --noEmit` clean on `driver-app`. Real gaps, not yet done: (a) no state-specific daily-OT rules (e.g. California's 8hr/day threshold), federal 40hr/week only; (b) a workweek split across two pay periods only sees the hours visible in the period being computed — a real payroll system should be the authority on cross-period OT, not this estimate; (c) I-9/W-4 deliberately **not** built into `DriverDocument`/`DocumentsScreen` — that's Rippling's own onboarding's job (with real federal I-9 retention/audit requirements this app shouldn't reimplement), not a parallel tracking system here; (d) Phase 2 (1099) and Phase 3 (gig) of the rollout haven't started — Phase 2's main remaining item is a legal decision (how much real job-choice/schedule-autonomy the offer model needs to show for defensible 1099 classification), not code; Phase 3 needs a real per-delivery fare model that doesn't exist anywhere in `Order`/`Route`/`Stop` yet. |

## Business / Org

These aren't code tasks, but they gate what the code above is even for —
worth tracking alongside engineering, not separately.

| # | Item | Why it matters | Status |
|---|---|---|---|
| 7 | Hire the senior backend engineer | Peer review names this the critical path — "do not hire down." | Not started |
| 8 | Sign the first client contract (before or alongside the hire above) | Without it, there's no real data to shadow-test Hub 1 against, and the 2.5 DPH figure stays a model assumption instead of something proven live. **Not a blocker for the rest of the engineering list** — only item 2 (and possibly item 3, if the Source of Truth Index specifically needs a design partner rather than just Google Drive access) actually depends on a signed client. Everything else above can and should proceed in parallel using placeholder/demo data (see `demo/`). | Not started |

## How to use this file

- Update Status in place: `Not started` → `In progress` → `Done`.
- If an item spawns sub-tasks, add a nested list under that row rather than
  creating a new top-level row — keep this file scannable in one pass.
- When an engineering item is closed, cross-check whether the corresponding
  caveat in `docs/ARCHITECTURE.md` ("stubbed" / "best-effort interpretation"
  sections) should be updated or removed.
